# YouTube Summarizer Bot - 프로젝트 개요

## 📁 프로젝트 구조

```
001_discord_to_notion/
├── 📄 README.md                    # 상세 문서
├── 📄 QUICKSTART.md                # 15분 빠른 시작 가이드
├── 📄 PROJECT_OVERVIEW.md          # 이 파일
├── 📄 .env                         # 환경변수 (Git 제외)
├── 📄 .gitignore                   # Git 제외 파일 목록
├── 📄 requirements.txt             # Python 의존성
├── 📄 main.py                      # Cloud Functions 메인 Worker
├── 📄 deploy.sh                    # 배포 스크립트
├── 📄 supabase_schema.sql          # Supabase 데이터베이스 스키마
│
├── 📂 core/                        # 핵심 모듈
│   ├── __init__.py
│   ├── youtube_info.py             # YouTube 정보 추출
│   ├── subtitle_extractor.py       # 자막 추출
│   ├── ai_summarizer.py            # AI 요약 (Gemini/Claude)
│   └── notion_saver.py             # Notion 저장
│
├── 📂 cloudflare-worker/           # Telegram Webhook Handler
│   ├── index.js                    # Worker 코드
│   ├── wrangler.toml               # Cloudflare 설정
│   └── package.json                # npm 의존성
│
└── 📂 old/                         # 기존 Discord 봇 코드 (백업)
    ├── main.py                     # 기존 Discord WebSocket 방식
    ├── README.md                   # 기존 문서
    └── ...
```

---

## 🏗️ 아키텍처 다이어그램

### 전체 흐름

```
┌─────────────────┐
│  📱 Phone       │
│  (YouTube 공유) │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 🤖 Telegram Bot │
│  (Webhook)      │
└────────┬────────┘
         │ HTTP POST
         ▼
┌─────────────────┐
│ ☁️ Cloudflare   │
│   Workers       │ ← 역할: URL 수신 & Supabase 등록
│  (무료 100K/일)  │
└────────┬────────┘
         │ INSERT
         ▼
┌─────────────────┐
│ 🗄️ Supabase     │
│   (PostgreSQL)  │ ← 역할: 작업 큐
│  jobs 테이블     │
└────────┬────────┘
         │ 1분마다 polling
         ▼
┌─────────────────┐
│ ⏰ Cloud        │
│   Scheduler     │ ← 역할: 1분마다 트리거
│  (무료 3 jobs)  │
└────────┬────────┘
         │ HTTP GET
         ▼
┌─────────────────────────────────┐
│ ☁️ Cloud Functions              │
│  (Python Worker)                │
│                                 │
│  ┌─────────────────┐            │
│  │ 1. YouTube Info │            │
│  │    Extraction   │            │
│  └────────┬────────┘            │
│           │                     │
│  ┌────────▼────────┐            │
│  │ 2. Subtitle     │            │
│  │    Extraction   │            │
│  └────────┬────────┘            │
│           │                     │
│  ┌────────▼────────┐            │
│  │ 3. AI Summary   │            │
│  │   (Gemini/      │            │
│  │    Claude)      │            │
│  └────────┬────────┘            │
│           │                     │
│  ┌────────▼────────┐            │
│  │ 4. Notion Save  │            │
│  └────────┬────────┘            │
│           │                     │
│  ┌────────▼────────┐            │
│  │ 5. Telegram     │            │
│  │    Notification │            │
│  └─────────────────┘            │
└─────────────────────────────────┘
         │
         ▼
┌─────────────────┐
│ 📱 Telegram     │
│   (완료 알림)    │
└─────────────────┘
         │
         ▼
┌─────────────────┐
│ 📄 Notion       │
│   (요약 저장)    │
└─────────────────┘
```

---

## 💾 데이터 흐름

### 1. 사용자 → Telegram

```json
{
  "message": {
    "chat": {"id": 7416961176},
    "text": "https://www.youtube.com/watch?v=dQw4w9WgXcQ"
  }
}
```

### 2. Telegram → Cloudflare Worker

```javascript
// Webhook POST
{
  "update_id": 12345,
  "message": {...}
}
```

### 3. Cloudflare → Supabase

```sql
INSERT INTO jobs (
  youtube_url,
  telegram_chat_id,
  channel,
  status
) VALUES (
  'https://www.youtube.com/watch?v=...',
  7416961176,
  'archive',
  'pending'
);
```

### 4. Cloud Scheduler → Cloud Functions

```
GET https://asia-northeast3-n8n-ai-work-agent-automation.cloudfunctions.net/process-youtube-jobs
```

### 5. Cloud Functions → Supabase (조회)

```sql
SELECT * FROM jobs
WHERE status = 'pending'
ORDER BY created_at
LIMIT 5;
```

### 6. Cloud Functions → YouTube API

```
GET https://www.googleapis.com/youtube/v3/videos?id=dQw4w9WgXcQ
```

### 7. Cloud Functions → Gemini API

```json
{
  "contents": [{
    "parts": [{
      "text": "영상 제목: ...\n자막: ..."
    }]
  }]
}
```

### 8. Cloud Functions → Notion API

```json
{
  "parent": {"database_id": "290b59..."},
  "properties": {
    "Name": {"title": [{"text": {"content": "영상 제목"}}]},
    "URL": {"url": "https://..."}
  },
  "children": [...]
}
```

### 9. Cloud Functions → Telegram API

```json
{
  "chat_id": 7416961176,
  "text": "✅ 요약 완료!\n📄 Notion: ..."
}
```

---

## 🔑 주요 기술 스택

| 계층 | 기술 | 역할 | 비용 |
|------|------|------|------|
| **트리거** | Telegram Bot API | 사용자 인터페이스 | 무료 |
| **Webhook** | Cloudflare Workers | 요청 수신 & 작업 등록 | 무료 (100K/일) |
| **데이터베이스** | Supabase (PostgreSQL) | 작업 큐 & 상태 관리 | 무료 (500MB) |
| **스케줄러** | Cloud Scheduler | 1분마다 Worker 호출 | 무료 (3 jobs) |
| **Worker** | Cloud Functions (Python) | 실제 작업 처리 | 무료 (400K GB-s) |
| **YouTube** | YouTube Data API v3 | 영상 정보 | 무료 (10K/일) |
| **자막** | youtube-transcript-api | 자막 추출 | 무료 |
| **AI** | Gemini 2.0 Flash | AI 요약 (메인) | 무료 (1500/일) |
| **AI 백업** | Claude 3 Haiku | AI 요약 (백업) | 유료 (저렴) |
| **저장** | Notion API | 최종 저장소 | 무료 |

---

## 📊 성능 지표

### 처리 시간

| 단계 | 평균 시간 | 설명 |
|------|----------|------|
| Telegram → Cloudflare | 0.1초 | 즉시 응답 |
| Cloudflare → Supabase | 0.2초 | 작업 등록 |
| Supabase → Cloud Functions | 1분 | Scheduler 간격 |
| YouTube Info | 1초 | API 호출 |
| Subtitle Extraction | 2-5초 | 자막 길이에 따라 |
| AI Summary (Gemini) | 10-30초 | 자막 길이에 따라 |
| Notion Save | 2-5초 | 블록 개수에 따라 |
| Telegram Notification | 0.5초 | 메시지 전송 |
| **총 처리 시간** | **1-2분** | Scheduler 지연 포함 |

### 리소스 사용량 (월 100회 기준)

| 서비스 | 사용량 | 무료 한도 | 사용률 |
|--------|--------|----------|--------|
| Cloudflare Workers | 200 req | 100K req/일 | 0.007% |
| Supabase DB | 10MB | 500MB | 2% |
| Cloud Functions | 100 GB-s | 400K GB-s | 0.025% |
| YouTube API | 100 req | 10K req/일 | 0.03% |
| Gemini API | 100 req | 1500 req/일 | 0.2% |

---

## 🔒 보안

### API 키 관리

- **GCP Secret Manager**: 모든 민감한 키 저장
- **Cloudflare Secrets**: Telegram Token, Supabase Key
- **환경변수 분리**: `.env` 파일은 Git 제외

### 데이터 보호

- **Supabase RLS**: Row Level Security 활성화
- **Service Role만 접근**: 일반 사용자는 읽기만 가능
- **HTTPS 통신**: 모든 API 통신 암호화

### 권한 제한

- **Cloud Functions**: 필요한 Secret만 접근
- **Notion Integration**: 특정 Database만 접근
- **Telegram Bot**: Webhook만 사용 (polling 없음)

---

## 🆚 기존 버전과 비교

| 항목 | v1.0 (Discord WebSocket) | v3.0 (Telegram Serverless) |
|------|-------------------------|---------------------------|
| **트리거** | Discord Gateway | Telegram Webhook |
| **상시 실행** | ✅ VM 24/7 | ❌ 필요 시만 |
| **비용** | $25/월 | $0/월 |
| **확장성** | 1 VM 제한 | 자동 스케일링 |
| **장애 복구** | 수동 재시작 | 자동 재시도 |
| **모바일 편의성** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **설정 복잡도** | 낮음 | 중간 |
| **유지보수** | VM 관리 필요 | 자동 관리 |

---

## 📈 향후 개선 사항

### Phase 4 (선택)

- [ ] **Web 프론트엔드**: Vercel + Next.js로 웹 UI 추가
- [ ] **Discord 재연동**: Slash Command 방식으로 전환
- [ ] **Slack 봇**: Slack Slash Command 지원
- [ ] **멀티 채널 확장**: 더 많은 Notion Database 지원
- [ ] **실시간 진행률**: Supabase Realtime으로 처리 상태 구독
- [ ] **통계 대시보드**: 작업 통계, 성공률, 평균 처리 시간 등

### 최적화

- [ ] **캐싱**: 동일 영상 재요청 시 캐시 사용
- [ ] **병렬 처리**: 여러 작업 동시 처리 (현재는 순차)
- [ ] **자막 압축**: 긴 자막을 더 효율적으로 처리
- [ ] **오류 복구**: 실패 작업 자동 재시도 로직 개선

---

## 📚 참고 문서

### 공식 문서

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Cloudflare Workers](https://developers.cloudflare.com/workers/)
- [Supabase Documentation](https://supabase.com/docs)
- [GCP Cloud Functions](https://cloud.google.com/functions/docs)
- [YouTube Data API](https://developers.google.com/youtube/v3)
- [Gemini API](https://ai.google.dev/docs)
- [Notion API](https://developers.notion.com/)

### 내부 문서

- [README.md](README.md) - 상세 설치 가이드
- [QUICKSTART.md](QUICKSTART.md) - 15분 빠른 시작
- [old/README.md](old/README.md) - 기존 Discord 봇 문서

---

## 🐛 알려진 이슈

1. **자막 없는 영상 처리 불가**
   - 해결: 자막이 있는 영상만 지원 (현재)
   - 향후: 영상 설명 또는 음성 인식 추가 예정

2. **1분 지연**
   - 원인: Cloud Scheduler가 1분마다 실행
   - 해결: Pub/Sub 방식으로 전환 시 즉시 처리 가능
   - 현재: 비용 최적화를 위해 1분 지연 허용

3. **긴 영상 처리 시간**
   - 원인: 자막이 매우 길면 AI 처리 시간 증가
   - 해결: 자막 길이 제한 (24,000자)
   - 향후: 청크 단위 처리 고려

---

## 💬 FAQ

**Q: 왜 Discord에서 Telegram으로 바꿨나요?**
A: Discord WebSocket 방식은 VM이 필요해서 월 $25가 들었습니다. Telegram Webhook은 VM 없이 완전 무료로 운영 가능합니다.

**Q: 여전히 Discord를 사용하고 싶어요.**
A: `old/` 폴더에 기존 코드가 있습니다. 또는 Discord Slash Command로 전환하면 서버리스로 운영 가능합니다.

**Q: Gemini API가 실패하면 어떻게 되나요?**
A: 자동으로 Claude Haiku로 전환됩니다 (유료지만 저렴).

**Q: 하루에 몇 개까지 처리할 수 있나요?**
A: 무료 티어 기준:
- Gemini: 1500개/일
- YouTube API: 10,000개/일
- Cloudflare: 100,000개/일

실질적으로 Gemini의 1500개/일이 한계입니다.

**Q: 한국어가 아닌 다른 언어도 지원하나요?**
A: 네, youtube-transcript-api가 지원하는 모든 언어의 자막을 추출할 수 있고, Gemini가 한글로 번역합니다.

---

**마지막 업데이트**: 2025년 11월 3일
**버전**: 3.0.0
